name: CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v1
    - name: Run a one-line script
      run: echo Hello, bitch!
    - name: Run a multi-line script
      run: |
        echo Add other bitches to build,
        echo test, and fuck up your project.
